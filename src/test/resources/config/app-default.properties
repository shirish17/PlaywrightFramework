############################################
# APP-LEVEL DEFAULTS
# Lowest priority - overridden by test-default, env-specific, and CLI
############################################

# Browser (default: chrome)
browser=chrome

# Headless mode (default: false = show browser)
headless=false

# Timeouts (in milliseconds)
timeout.element=10000
timeout.page=30000